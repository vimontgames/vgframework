name: "Win64 DX12 Release"

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]
    paths:
      - '**/*.vcxproj'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.c'
      - '**/*.cpp'
      - '**/*.inl'
      - '**/*.hlsli'
      - '**/*.hlsl.h'
  pull_request:
    branches: [ "master" ]
    paths:
      - '**/*.vcxproj'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.c'
      - '**/*.cpp'
      - '**/*.inl'
      - '**/*.hlsli'
      - '**/*.hlsl.h'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      build_name: "Win64 DX12 Release"
      build_platform: "Win64 DX12"
      build_configuration: "Release"
    secrets:
      DISCORD_BUILD_WEBHOOK_URL: ${{ secrets.DISCORD_BUILD_WEBHOOK_URL }}
